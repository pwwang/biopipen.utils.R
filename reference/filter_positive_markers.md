# Keep only the positive markers of a marker table

Keep only the positive markers of a marker table

## Usage

``` r
filter_positive_markers(df)
```

## Arguments

- df:

  A canonical marker table.

## Value

The marker table with the negative-direction markers removed.
