# patch to fix feature names not attached to the raw data

patch to fix feature names not attached to the raw data

## Usage

``` r
.H5SeuratToH5AD(
  source,
  dest,
  assay = DefaultAssay(object = source),
  overwrite = FALSE,
  verbose = TRUE
)
```
