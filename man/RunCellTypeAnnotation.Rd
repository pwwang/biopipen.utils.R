% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrna-celltype-annotation.R
\name{RunCellTypeAnnotation}
\alias{RunCellTypeAnnotation}
\title{Run a cell type annotation tool}
\usage{
RunCellTypeAnnotation(
  object,
  tool,
  args = list(),
  ident = NULL,
  cache = NULL,
  log = NULL
)
}
\arguments{
\item{object}{Seurat object, or a path to an RDS/qs/qs2/h5ad/h5seurat file to
read the object from.}

\item{tool}{One of the supported tool names, see
\code{\link[=celltype_annotation_tools]{celltype_annotation_tools()}}.}

\item{args}{Named list of tool arguments, e.g. \code{cell_types} for the \code{direct}
tool.}

\item{ident}{Metadata column with the clusters, required by cluster-level
tools, optional for cell-level ones.}

\item{cache}{Directory for conversions and tool scratch files, default
\code{tempdir()}.}

\item{log}{Logger.}
}
\value{
A list with:
\itemize{
\item \code{mapping}: a named list mapping each cluster to its cell type, or for
\code{type = "cell"} a data.frame of per-cell labels (cell IDs as row names, the
annotation in the first column).
\item \code{type}: \code{"cluster"} when the result is per cluster, \code{"cell"} when it is per
cell.
\item \code{cells}: the per-cell data.frame when \code{type = "cluster"} but the tool also
produced per-cell labels, \code{NULL} otherwise.
\item \code{more}: extra cluster mappings when the tool supports them (e.g. the
\code{more_cell_types} of the \code{direct} tool), \code{NULL} otherwise.
}
}
\description{
A unified interface over the cell type annotation tools used by biopipen's
\code{CellTypeAnnotation} process. The tool is picked by \code{tool} and its
tool-specific parameters are passed in \code{args}; see
\code{\link[=celltype_annotation_tools]{celltype_annotation_tools()}} for the supported tools and the level each of
them annotates at.
}
\details{
Cluster-level tools (the ones with \code{level = "cluster"}) need \code{ident}, the
metadata column holding the clusters, and label each cluster as a whole.
Cell-level tools label individual cells; when \code{ident} is also given, the
per-cell labels are aggregated to cluster-level ones by majority vote.
}
\examples{
\donttest{
obj <- SeuratObject::pbmc_small  # clusters in `groups`: g1, g2, g3
rec <- RunCellTypeAnnotation(
    obj, "direct",
    args = list(cell_types = list(g1 = "T", g2 = "B", g3 = "Mono")),
    ident = "groups"
)
rec$type
rec$mapping
}
}
